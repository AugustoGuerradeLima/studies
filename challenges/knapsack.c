#include <stdio.h>

/*
conjunto - todos os numeros possiveis
subconjunto - sera retornado apenas um deles e o somatorio e target
target - numero procurado a cada chamada, quando somados e subtraimos o primeiro target chegamos ao target
i - conta o index do subconjunto para armazenar os elementos do somatorio
*/
int knapsack(int*conjunto, int*subconjunto, int target, int i){

    //oq pensei ate entao
    /*procura no conjunto o target, nao pega numeros maiores que o target inicialmente
      se encontra o target esse sera o subconjunto
      caso contrario encontra o numero maior que e menor que target
      entao subtrai o numero encontrado de target e faz uma chamada recursiva com o resultado
      faz isso ate que seja 0
    */

}
int main(){
    return(0);
}